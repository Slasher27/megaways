---
interface Props {
	variant?: 'default' | 'outlined' | 'elevated' | 'slot';
	padding?: 'none' | 'sm' | 'md' | 'lg';
	hover?: boolean;
	class?: string;
}

const {
	variant = 'default',
	padding = 'md',
	hover = false,
	class: className = '',
} = Astro.props;

const variants = {
	default: 'bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700',
	outlined: 'bg-transparent border-2 border-gray-200 dark:border-gray-700',
	elevated: 'bg-white dark:bg-gray-800 shadow-lg',
	slot: 'bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700',
} as const;

const paddings = {
	none: '',
	sm: 'p-4',
	md: 'p-6',
	lg: 'p-8',
} as const;

const hoverClasses = hover
	? variant === 'slot'
		? 'transition-all duration-200 hover:shadow-lg'
		: 'transition-all duration-200 hover:shadow-xl hover:scale-[1.02] cursor-pointer'
	: '';
---

<div
	class:list={[
		'rounded-xl',
		variants[variant],
		paddings[padding],
		hoverClasses,
		className,
	]}
>
	<slot />
</div>
